node_modules/
.next/
out/
build/
.env
.env*.local
.DS_Store
*.log
.vercel
next-env.d.ts
/prisma/*.db
.demo-data/
.rehost-backup/
.env*
# Postgres dumps of migrated stores — customer PII and auth hashes. Kept local
# only; the legacy order import reads one from the repo root.
*.backup

# Stale build output kept aside during a broken-build recovery. Never commit.
.next-stale-backup/
